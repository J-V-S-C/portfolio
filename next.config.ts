import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: ['cdn-icons-png.freepik.com', 'cdn-icons-png.flaticon.com'],
  },
  /* config options here */
};

export default nextConfig;
